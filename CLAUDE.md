# Project instructions

- Never run `git commit` or `git push` in this repo, even with explicit confirmation in the moment. The user (daniel-fallows) handles all commits and pushes themselves. Stage/prepare changes if asked, but stop before committing — always leave the commit/push step to the user.
