# Normalize line endings to LF in the repo, check out LF on all platforms
* text=auto eol=lf

# Treat these as binary — never touch their line endings
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
